version: 2

models:
  - name: stage_payments
    columns:
      - name: payment_id
        tests:
          - unique
          - not_null
      - name: order_id
          - not_null
      - name: status
        tests:
          - accepted_values:
              values:
                - fail
                - success